
# Categorical bans
**/.git/**

# Directory bans
.cache/

# Duplication
go/
rust/
wallpaper/.wallpapers/**

# Personal
wiki/
sound/
python/
mozilla/
local/
vim/.vim/

# MPD system info
config/.config/mpd/db
config/.config/mpd/pid
config/.config/mpd/state
config/.config/pulse/

# Torrent client info
config/.config/transmission/

# Systemd info but not custom units
config/.config/systemd/user/default.target.wants/

# Coc.nvim info
config/.config/coc/extensions/*
config/.config/coc/history.json
config/.config/coc/memos.json
config/.config/coc/mru

# Ignore openttd save files
openttd/.openttd/save/

# Filetype bans
*.tdb
*.log
*.torrent
*.resume
credentials.*
credentials

# File bans
vim/.viminfo
less/.lesshst
bash/.bash_history
bash/.bashrc
feh/.fehbg
config/.config/dconf/user

# Ignore node stuff
config/.config/yarn/
node/.node_repl_history
yarn/.yarn/

# Docker login details
docker/.docker/config.json

# Ignore task due to being a separate repo from now on
task/*
# Keeping the config tho
!task/.taskrc

# Ignore most of minikube except config
k8s/.minikube/*
!k8s/.minikube/config
k8s/.minikube/config/*
!k8s/.minikube/config/config.json

# Ignore secrets
env/.env

# Ignore aws config as this could leak info
aws/.aws/config

# Don't ignore the folder, just the directory
!OSX
OSX/*

# Database configuration files -- system dependent
mssql/*
pg/*
