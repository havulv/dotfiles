#!/usr/bin/env bash
# ~/.bash_profile
# Note that the bash profile is only executed on login, and the bashrc is
# executed for every bash session. So, if you are exporting an env var for
# every session, you should put it into bashrc and not here. See the man
# page for more details.
